# `b_balances`

Write a dapp that reads the wallet balances of the tokens of the pivot pool, so,
for example, a wallet has AVAX, BTC, and ETH, and the pivot pool on the wallet
is BTC+ETH, so you'd read the BTC and ETH token-balances.

Use whatever library functions that will asset.

* [solution](mod.rs)

