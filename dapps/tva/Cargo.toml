[package]
name = "tva"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "tva"
path = "../../quizzes/src/quiz01/tva/src/main.rs"

[dependencies]
quizzes = { path = "../../../protocol/quizzes" }
book = { path = "../../../crypto-n-rust/src/libs/book" }
libs = { path = "../../../protocol/libs" }

tokio = { version = "1.53.1", features = ["macros", "rt-multi-thread"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(tarpaulin_include)'] }
